{
  "name": "resume",
  "version": "1.0.0",
  "description": "Christopher West's Resume",
  "private": true,
  "scripts": {
    "start": "browser-sync start --server --files '*.html, css/*.css, js/*.js, templates/*.html, resume.json' --no-notify --no-ghost-mode"
  },
  "devDependencies": {
    "browser-sync": "^2.29.3"
  }
}
