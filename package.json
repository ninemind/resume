{
  "name": "resume",
  "version": "1.0.0",
  "description": "Christopher West's Resume",
  "private": true,
  "scripts": {
    "start": "browser-sync start --server --files '*.html, css/*.css, js/*.js, templates/*.html, resume.json' --no-notify --no-ghost-mode",
    "build": "mkdir -p dist && cp -r css js templates resume.json index.html dist/"
  },
  "dependencies": {
    "bootstrap": "^3.4.1",
    "font-awesome": "^4.7.0",
    "jquery": "^3.5.0",
    "mustache": "^4.2.0",
    "serve": "^14.2.1"
  },
  "devDependencies": {
    "browser-sync": "^2.29.3"
  }
}
